name: 'Greet issues'
description: 'This action will greet people when they create a new issue'
author: 'Lenin'
inputs: 
  myInput:
    description: 'Input to use'
    default: 'world'
runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - ${{ inputs.myInput }}
